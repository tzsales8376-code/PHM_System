<Window x:Class="Tranzx.iVS4.App.Views.EventLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource EventLog.Title}"
        Width="900" Height="600"
        Background="{DynamicResource BgPrimary}" Foreground="{DynamicResource TextPrimary}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{DynamicResource EventLog.Title}"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{DynamicResource AccentTeal}"/>
            <Button Grid.Column="1" Content="📂"
                    Padding="10,4" Click="OnOpenFolder"
                    Background="{DynamicResource BgTertiary}"
                    Foreground="{DynamicResource TextPrimary}"
                    ToolTip="{DynamicResource EventLog.OpenFolder}"/>
        </Grid>

        <TextBlock Grid.Row="1" x:Name="lblFolder"
                   FontFamily="Consolas" FontSize="11"
                   Foreground="{DynamicResource AccentBlue}"
                   TextTrimming="CharacterEllipsis" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="2" x:Name="grid"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  IsReadOnly="True"
                  AlternatingRowBackground="{DynamicResource BgTertiary}"
                  Background="{DynamicResource BgSecondary}"
                  Foreground="{DynamicResource TextPrimary}"
                  RowBackground="{DynamicResource BgSecondary}"
                  BorderBrush="{DynamicResource BgTertiary}"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource EventLog.Time}" Width="160"
                                    Binding="{Binding Time, StringFormat=yyyy-MM-dd HH:mm:ss.fff}"/>
                <DataGridTemplateColumn Header="{DynamicResource EventLog.Level}" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="6,2" HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#3A3A50"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding LevelText}" Value="WARN">
                                                <Setter Property="Background" Value="#F39C12"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding LevelText}" Value="ERROR">
                                                <Setter Property="Background" Value="#E74C3C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding LevelText}" Foreground="White"
                                           FontWeight="SemiBold" FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="{DynamicResource EventLog.Source}" Width="140"
                                    Binding="{Binding Source}"/>
                <DataGridTextColumn Header="{DynamicResource EventLog.Message}" Width="*"
                                    Binding="{Binding Message}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
